{% extends 'base.html.twig' %}

{% block title %}
	{{ articles.title }}
	|
	{{ site_name }}
{% endblock %}

{% block body %}
	<section id="show-art">
		<div class="container">
			<h2>
				{{ articles.title }}
			</h2>
			<img
			src="{{ asset('article_images/') }}{{ articles.imageName }}" alt="{{ articles.imageName }}">
		</div>
	</section>
{% endblock %}
