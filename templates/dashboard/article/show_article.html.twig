{% extends 'base.html.twig'%}

{% block body%}
<div class="content">
    <h1>{{ article.title}}</h1>
    <h2>{{ article.user}}</h2>
    <p>{{ article.description}}</p>
    <p>{{ article.content}}</p>
</div>
{% endblock body%}