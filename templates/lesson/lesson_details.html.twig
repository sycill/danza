{% extends 'base.html.twig'%}

{% block body%}
	<div class="courses">
		<div class="course">
			<h2>{{ lesson.title }}</h2>
			<p>{{ lesson.description|raw}}</p>
			<p>{{ lesson.teacher}}</p>
			<p>{{ lesson.teacher.lesson }}</p>
		</div>
	</div>
{% endblock %}
